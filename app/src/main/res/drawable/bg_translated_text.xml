<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorSurface" /> <!-- Or use a slightly different shade if available, standard surface is fine or #10000000 tint -->
    <!-- Let's use a explicit semi-transparent generic or just surface_card -->
    <solid android:color="@color/surface_card"/>
    <corners android:radius="12dp" />
    <stroke
        android:width="1dp"
        android:color="@color/text_secondary" />
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>
